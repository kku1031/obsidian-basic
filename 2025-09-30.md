![[Pasted image 20250930153453.png]]

![[Pasted image 20250930153518.png]]

커서안에 있으면 터미널, cmd 창에 있으면 cmd 절대 경로가 같음 

![[Pasted image 20250930154914.png]]

Main.class와 Main.java 실행 시, 자바 언어에 저장 된다. 컴퓨터 저장소는 디스크, 메모리, 

소스 코드라 부르는 이유 : 소스 (클래스 파일의 원천)

class main 일때  디폴트 값이니까, private(외부 불허용)
public class main 일때, 퍼플릭

컴퓨터가 받아 들일때 거울처럼 반전으로 받아 들임 -. java Main이
명령어 일때 파일명은 Main.java


{}
![[Pasted image 20250930160727.png]]
scope(영역=땅) 클래스 클래스의 컬 브레스 

main.함수(x) 는 안이 감춰짐 
클래스 안에 있는거 메소드(방법)

System.out : 터미널에 보이는거 모니터에 출력해라 ,
System.in : 키보드

자바는 메소드를 취함.

void는 메소드에서 끝, 종점이라는 의미

외부에서 입력값을 받게 하는 것 스캐너

자바를 클래스 파일로 바꾸는 것 : 컴파일

깃허브
git init 
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/kku1031/java-basic.git
git push -u origin main

git config --global user.email "kku1031@naver.com"
git config --global user.name "kku1031"

