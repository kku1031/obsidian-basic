- deprecated - 더이상 사용되지 않음
- -> 실무적으로는 App Router 방식(서버 컴포넌트 + fetch 캐싱)으로 전환하는 방식 추천
- 서버 컴포넌트 : `리액트 컴포넌트`를 브라우저가 아니라 서버에서 렌더링하는 방식
- Node.js(2009년 구글, 안드로이드에 최적화)와 자바스크립트와 형태는 같으나 는 서버와 클라이언트에서 전혀 다르게 동작한다.
- 백엔드는 노드
- Node.js - 비동기(모바일이 등장하면서 그 동안은 동기처리 했던 것들이 비동기화로 바뀜)
- Node.js의 모듈(파일 분할)을 설명합니다. - 구조, 할당, 분해 
- 비동기 코드의 4가지 패턴 
  
- package.json - 의존 관계를 관리(gradle과 같음)
- /{id}를 붙여 라우트로 정의 : URL 경로 일부를 “동적인 값(파라미터)”으로 사용한다
- 리액트에서의 핵심 : 라우팅과 미들웨어 - 컨텍스트 연결하는함수
- 미들 웨어는 함수다. 함수는 순수, 람다(화살표)
- next(다음 미들웨어를 호출한다 (=콜)), 내뒤에도 콜할게 있어 (콜백)을 
- 순수 함수는 호출만 가능, 람다는 콜백 가능(ex 이벤트) 

- 자바 - 순수(메소드), 함수
- JS - 순수 함수, 람다 
- 리액트에서 use 있으면 훅/use 없으면 핸들러
- 리액트 -순수 함수가 `훅`이고 람다개념이 컨테이너의 `헨들러`(onClick={testAlert})임
- 클릭 기능을 해야,  {}안에 꺼를 호출하니까 기능이 2번 들어감.(콜백 개념)
- 화면에서 인풋값은 스트링만. 

- import axios from "axios" ; 남이 만든거 가져옴 
- import {useStore} from "@(src)/store" ; @ 내가 만든거 가져옴 커스텀 훅
-  ![[Pasted image 20251120112339.png]]
- 
- 모든 언어는 순수, 순수하지않은거(람다)로 나누고
- 리액트는 화면이니까 훅과 핸들러로 나눈다. 순수로 짤수도 있고 람다로 짤수도 있음.
- BP는 다른 문제 : 연속적인 건가, 연속적이지 않았다. ex) 송금, 인증
-  
- const 타입 var(변수) const(상수 - 기능은 다) 2개 있으면 생략 불가능 하나 있으면 생략 가능.
-  ![[Pasted image 20251120115032.png]]
- 1번 동기 (디폴트) : 데이터 바로바로 전달 , 계속 떠있어야함, 2번 async 비동기
- await
- = : 값 대입
- == : 같다 정수만 
- === :  스트링만, 화면으로 입력된거 전부 자연어
- => 자바에서 .equals
- 
- 3항 연산자 ? 맞으면 일로 : 틀리면 여기로
- 
- undefined(js)
- null
- ![[Pasted image 20251120124706.png]]
- 
- 자바에서 가비지 컬렉터가 필요 없는 것들을 제거 하니까 undefined가 필요가 없고
- 리액트에서는 가비지 컬렉터가 없으니까 undefined로 지정해줘야 쓰레기인줄 인식 하고 버림.
- null쓸건데 나중에 쓸거니까 지우지마셈. DB는 나중에 채워야 하니까 null.
- 
- undefined가(쓰레기), null(비어있다)
- ?: ,옵셔널(선택적) 있어도 되고 없어도 된다, 널 허용
- ??, Null 병합 연산자  undefined나 null, 비어만 있으면 허용 한다. 
- A ?? B undefined나 null, 비어만 있으면 B, 그외면 A
- ?. , 값이 null/undefined이면 에러 내지 않고 그냥 undefined를 반환함
- 객체가 비어있으면 null , null point(주소값이 없다) exception 
- !message 비어있으면


- 자바는 파이프라인을 연결해야 동작하고 전달해야함
- 방향성이 없음, 연결 X, 전달 할 필요가 없음
- store랑 slice 결국 한몸이고 구역으로 나눈거임 자바랑 다름.

- (  )=>{  }  함수, 함수인데 이름이 없는거 (어나니머스)
- {  } JSON - 자바스크립트 객체에서 노테이션만 있음, 메모리에 주소할당안하는데 값은 있다.
- 클래스, 클래스에서 이름이 없는거(JSON)
- 
- JSON 2종류 [ ]백터 , {  } 스칼라  {ai[ ]}

- 
-  
-  
- 
- 
-  
-  
-  
- 
- 
-  
-  
-  
- 
- 
-  

