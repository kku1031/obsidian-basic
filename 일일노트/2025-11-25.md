- ![[Pasted image 20251125100740.png]]
- 기존에는 위 그림과 같은 형태였는데 하나의 흐름 구조(한강만 흐르고 있었는데, ai라는 청계천 등등 etc로 점점 살 붙음) 였는데, AI 시대가 되면서 하나의 GW에 여러개가 붙음.
- 여기서 gateway를 gof 파사드 (성문 열면 또 마차타고 20분 가는거 마냥 `문짝`임) 라고함.
- 그래서 여러개를 사용하기 위해 flux를 붙인거. 기존에는 API라고 부르던 것들에 AI 개념이 
- 추가되면서 Agent라고 따로 명명함. 기존 API는 API라고 부르고 AI가 추가되면 Agent임 
- stream 동기 flux 비동기인데 카톡은 flux다.
- 흐름이 한군데에서만 국한 되는게 아니라, 여러 흐름이 흘러도 데이터를 받을 수 있게 됨.
- GW에 여러개가 붙어서 비대해지니까 비동기(Flux) 처리 하게됨.
- 신상 회사를 만들면 BE까지 flux로 만드는데, 큰 회사들은 BE는 그대로 두고 GW는 flux로 바꾼다. 당근, 토스 등은 flux 쓰고 있고 배민은 flux로 바꾸고 있음. 네이버는 아님. 
- ai는 데이터가 계속 흐르고 있는 상태(flux)에서 돌아가야함. docker 꺼졌다고 다꺼지는게 말이 안됨.  
- gateway에 키를 박아 넣으니까 gateway 보안이 개빡세게 들어가야함. env하긴 했는데 그걸로 부족함.
- 어제 한일 `Gateway -> flux로 바꿈.`  같은 도커에 넣어서 통신을 편하게 하기 위해서 기존의 시스템을 백엔드 안에 프론트에 넣음. 백이랑 프론트랑 따로 분리한 이유는 보안 강화 때문에.
- 
- 자연의 양방향성은 불가능하고 중간에 교차엔티티를 두어서 양방향으로 바꾸어준다. (진리) 
- 스웨거 기준으로 생각할때 스웨거 들어가는거 실패하면 팅기니까  FE <-> SW -> GW  이 구조가됨

- GW 하나만 만든 이유 : MSA서비스 1대 다 구조로 만들기 위해.
- 보안 : POST 방식이 필요함
- 백엔드 아마존 = EC2 = ECC , 프론트 (버셀) , 아마존, 버셀 경계선 - swagger
- ![[Pasted image 20251125103836.png|500]] 
-  게이트웨이가 플럭스니까 비동기 처리 할 수 밖에 없음.
- 
- head, body 기능이 없어서 객체는 아닌데 체는 됨. 마네킹. 기능이 추가되면 로보트
- 인덱스, 키가 기계어면(선형) , 인덱스가 없으면, 키가 자연어면(비선형)
- await fetch(가져오다, get) 프론트에서 쓰겠다, find 백
- 훅에 있는 정보(회원가입, 로그인 인증 정보)는 store에 저장된다. 
- 훅과 헨들러(store를 거칠 필요가 없는 것 : uri)의 차이
- 훅 스토어랑 내부 통신 할때만 쓰고(스토어), 그 외에서는 핸들러(스토어랑 핸들러는 연결 안되있음)는 화면
- 핸들러는 기본적으로 싱크
- 내부에서 훅한테 줄때는 한공간이니까 싱크, 플럭스가 등장해서 훅을 거치지 않는 데이터들이 많으니까 handler 중에 asy가 등장하고 붙으면 어싱크.

- ![[Pasted image 20251125112300.png|400]]
- cost a = (    ) => C : 람다 모양
- (b= null)이면 끊어진 상태 b에 진짜 값이 들어올때 반응함.
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 

