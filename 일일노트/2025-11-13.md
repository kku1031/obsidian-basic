- 특정 시점 :  state(current time) 결과 :status(의지 가지고 하다 에러남)
- 시스템을 만드는 입장에서는 status를 만들기 위해 state를 만드는 과정. 
- ![[Pasted image 20251113094955.png]] 
- eureka에서 instance : 하나의 MicroService, register = dependency
- 릴레이션쉽(연관관계) , 디펜던시(분리) <-> association 한몸
- ![[Pasted image 20251113100535.png]]
- 책임 : 종단, 관심 : 횡단(안쓰는거),  관심사에 따라 자르는거 `크로스 커팅`, 자동문에 선 시점 : `point`, 서서 문열어라고 코딩 하는거 : `advice`
- No instance : 등록은 되있는데 실행이 안된상태, 일일이 각각 해당하는 거 런 못누르니까 한방에 누르는게 필요함 => 그것이 `도커 `
- 여러개 인스터스 하나에 도커 하나 묶는거, 도커 컴포즈 
- 게이트웨이안의 디스커버리(라우터), 라우팅 : 분산 하는거, AOP(관심), 

-  ![[20251113_104030[1].jpg|300]]
- 유레카 -> 도커 -> 스프링부트 -> 포스트그리 
- 쿠버 인터페이스()
- 도커, 쿠버 언어가 야물로 같음
- 
-  json, yaml 같은 dsl언어의 특징과 철학은 무엇인가
-  공통 철학 : 로직(기능) 없이 데이터만 나타낸다
- =>함수가 없음
- => 데이터 직렬화 언어(원래는 병렬인데 직렬로 만든다)
- 물리적으로는 병렬(컨선), 논리적으로는 서비스 간 호출 체인이 생기면 부분적으로 직렬(책임)
- 병렬인놈을 직렬화
- JSON : 데이터를 직렬화(모든 데이터, 정형화된 것 뿐만 아니라, 비언어적 행동, 비정형화된 데이터들을 JSON)
- YAML : 컨네이너를 직렬화 -> 컨테이너가 직렬화가 된 전체가 파이프라인
- 컨테이너가 길, 그위에 달린게 데이터
- 저장소 : 릴레이션도 직렬화해야함,  자바에서 컨테이너 : 클래스 리액트 : 컴포넌트 - 직렬화해야함
- 언어 : `데이터`와 `그걸 담는 통` 2가지로 나뉨.
- 인간의 몸 : 하나로 쭉 이어져 있는데, 콜라 먹으면 콜라먹고 필요한 부분 쏙쏙 빼가는거 마냥 길은 전부다 직렬임(서비스가 텅 비어 있음), 각각의 소화기관에서 사용하는 걸 집어넣을 거임.
  컨
- ![[Pasted image 20251113121649.png]]
- 야물, 
- 노드(점), 엣지(연결)
- 유레카, 컨피그 = 단일 컨테이너 낱개로 있으면 연결 안하니까 네트워크를 걸어줘서 연결을 함.
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 

