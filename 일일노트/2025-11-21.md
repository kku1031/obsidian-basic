- ware - 물체(Something) -> 객체(아무개)다
- 미들웨어는 함수다
- 소프트웨어 <- 미들웨어(교차엔티티 개념) - fk 2개 -> 하드웨어
- 윈도우 OS 미들웨어 역할
- next쓰기 전에 express라는걸 씀 
- -> 레벨이 낮음 =>  성능이 다르다.
- 연쇄적(콜백) 
- 라우트 : 미들웨어의 묶음
- 컴포넌트 : 훅, 컨테이너 : 핸들러(콜백) -> 미들웨어역할 하는거 -> 자바
- 메서드 체인 응답
- -> 체인(책임 연쇄) -> 책임 들어간거 콜백 함수, 맨앞에꺼는 훅(고리여서),
  책임(기능), 
- 고리에 걸려서 줄줄 따라 옴.
- 메소드 체인(핸들러, 미들웨어, 자바) 포함
- 
- 미들웨어의 역할 
- 보안, 에러 헨들링, 헤더가 똑같으면 따로 줄 필요가 없으니까 `미들웨어가 헤더줌(http)` -> 우리는 바디만 하면됨.
- (예전부터 그랬으니 헤더랑 바디조합임)
-  
- next () 다음 함수로 넘기는거, 공통되는 부분 생략 -> next js(로 진화)에는 next()가 생략되있음
- 
- 책임에서 **SOLID** 원칙 (고체)
- 단일 책임 원칙(Single Responsibility Principle) SRP
- 책임 - 행동 - 메소드 - 기능 
- 하나의 클래스(또는 모듈)는 오직 하나의 책임(또는 변경 이유)만 가져야 한다.
- 하나의 메소드에 하나의 기능만 넣어서 분리시켜서 연결해라.
- 
- AOP(공통 로직을 분리한다)
-  
- 노테이션 - JSON, 제이슨은 헤더가 없네 -> 이름이 없네 -> 주소가 없네
- 
- 미들웨어는 HTTP(protocol)에서 규정한 헤더를 부여 -> http의 헤더만 주는 놈 존재.
- 쿠키처리 -> 쿠키만 주는 놈 존재.
- ex) 인증 허가, 클로드에 카카오 미들웨어 넣어줘.
- 
- 이런거 next js랑 스프링 사이의 미들웨어에 끼워넣으면 됨.
- ex) 개인토스(간단한거) 여러개 많이
- 미들웨어 쓰면 자바없이도 DB에 연결 되겠네?
- 연산이 필요없는 것들 ex)CEO 인삿말, 화면/ 은 그냥 바로 가져오면 됨.(광고가 먼저 나옴)
- 
- 미들웨어는 함수를 가져오는 느낌 API는 Key만 가져오는 느낌. 통신량이 많으면 돈 늘어남
- 보안성이 더 커짐. 깃허브 ssH
- 
- 미들웨어에서 {에러 자체를 안보내는거} 미들웨어로 에러 알람 설정
- 비동기웨어를 잡을 수 없다.
- 
- 경량 DB - 레디스, 포스트그리 DB 미들웨어에서 처리하는 정보는 레디스에 둔다.
- 레디스에 넣을 데이터 고민
- 포스트그리(중요한거, 1년치 재무재표)
- 
- 페이징 : 한번에 다가져오면 부하가 걸리니까 클릭할때 10건만 가져오고 그런식.
- 페이징 처리 전략이 필요함.
- 
- 도메인 빼고 : uri 전체 포함 url
- 
- undefined가(쓰레기), null(비어있다)
- ?: ,옵셔널(선택적) 있어도 되고 없어도 된다, 널 허용, 인터네페이스
- ??, Null 병합 연산자  undefined나 null, 비어만 있으면 허용 한다. 
- A ?? B undefined나 null, 비어만 있으면 B, 그외면 A
- ?. , 값이 null/undefined이면 에러 내지 않고 그냥 undefined를 반환함
- 객체가 비어있으면 null , null point(주소값이 없다) exception , 직접호출
- !message 비어있으면
- 
- Mycomponent : FC - 타입이 fc다
- : 널을 허용하지 않는다. 무조건 타입이 있어야한다 ?하나 붙여주면 널도 ㄱㅊ

-  리액트 컴포넌트 = FC(FunctionComponent)
- ![[Pasted image 20251121111804.png]]
- fc 함수라고 직접 쓰지마라. 
- 
- 자바의 핸들러 - 컨트롤러가 됨.
- 
- Gateway역할을 하는 별도 애플리케이션이 하나 더 생긴다고 이해하시는 게 맞습니다.
- 스프링이 flux를 따라감 -> 파이썬이 flux를 씀 
-  
- 스토어 - 데이터패턴 React Query - 
- 현재 있는걸 웹 플럭스로 구현을 하고 
- 그앞에 리액트 쿼리를 만들어서 연결해라 게이트웨이
- r2dbc - 웹플럭스
-  
- 도메인에 화면 띄우는 거 - 릴리즈
- 버셀(프론트 엔드 배포), 헤로쿠(백엔드 배포![[20251121_160834[1].jpg|500]])
-  
- 
- 발사믹 디자인(한두번 무료) - 캡쳐해서 피그마에 올려서 응애 해줘

- 버셀 테스트 화면
  
![[Pasted image 20251121190752.png|700]]
- 
- ![[Pasted image 20251121190716.png|700]]
