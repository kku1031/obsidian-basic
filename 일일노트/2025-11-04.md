- 방향 : 파이프라인(단방향, 양방향)
- 화면(컨, RequestMapping) - 서 -레 - 엔(매핑)
- 매핑이 아니면 인스턴스 
- ![[Pasted image 20251104095034.png]] 
- 인간의 언어는 " "로 감싸서 줘야함. 200개 언어 공통
- 리액->컨->엔->도커
- 지하철처럼 가다가 환승 하는 곳이 생김.
- 그 환승 작업 할거임 
-  @RestController : 화면과 컨트롤러가 분리 되어있음.
- ![[Pasted image 20251104101003.png]]
- DTO request(길), Entity나 VO, Messenger는 response 객체냐 개체냐 구분(@Getter)
- requst object(움직이는 놈), response object(움직이는 놈)
- Entity는 하나니까 생략해서 많이씀 엔티티는 하나, object는 여러개 엔티티가 부모 
- dto, vo란 말 없으니까 엔티티긴한데, 이름이 다름. 지금은 모름
- 실무에서도 dto, vo란 말 자체가 없는 경우 많음 구조로 판단해야함.
- 홈컨트롤러 : 접속이 잘됬다는 의미 
- ![[Pasted image 20251104104058.png]] 
-  ![[Pasted image 20251104104114.png]]
-  원래 메신저에서 @Data를 썻는데 Messenger는 리스폰스 니까 @Getter를 써야함 
- ![[Pasted image 20251104104358.png]]
- @NoArgsConstructor 세터넣음 , @AllArgsConstructor 세터 넣음, 파라미터 안에 넣고
- 메신저에 AllArgsConstructor 넣고 Messenger( )안에 넣음.
- `개중요 : s : 스칼라(return) f: 테이블 (인라인뷰, import) w: 조건(서브쿼리, @GetMapping,put 등등 어노테이션)  g: from의 집합 H : 0 자바는 
-  => 처리 속도를 높이기 위해서는 return을 스칼라로 바꾸면됨. new 는 임포트에서 왔으니까 from임![[Pasted image 20251104111437.png]]
- 3단 논법으로 이렇게 처리하면 처리 속도가 ㅈㄴ 빨라짐. 메모리를 안써.
- 메모리를 쓰는것 : 정석코드 , 안쓰는 것 : 암산(실무코드)
- home()은 추상의 의미니까 블록이라고 
- 스콥안에 스칼라 없음 , 자바에서는 뭐라할깡? 람다라고함.
- 데이터 마이그레이션 (Data Migration):
- 의미: 데이터를 한 저장소나 형식에서 다른 저장소나 형식으로 옮기는 작업입니다.    
- 예시: 기존의 온프레미스(사내 서버) 데이터베이스의 데이터를 클라우드 스토리지나 새로운 데이터베이스 시스템으로 옮기는 것.  좋은곳으로 이사하니까 -> `고도화`라고 불림.
-  타입은 var로 바꿔라 Messenger messenger = new Messenger();
- var entities = userRepository.findAll();
-  variable
- 모던자바 var키워드, 모듈시스템, 레코드(Records) - 최신의 개발트렌드, 현대적이고 효율적인 코드
- var키워드의 확장(jdk 10이상)
- 람다 : new도 안쓰고 메모리도 안씀(oop 진화 -> fp-함수형 프로그래밍)
- 생성 -> 객체
- 구조 -> 속성     => 엔티티 이걸로 gof디자인패턴으로 설계
- 행동-> 기능
- Messenger(객체니까 구조와 행동) messenger = new Messenger();(생성)
- 생성 : new를 제거하라(추,빌,팩,프로토타입,싱)
- 모놀리식 = 모든 기능이 한 서비스 안에 뭉쳐있다 (한 코드베이스, 한 빌드, 한 배포)
- 마이크로서비스 = 기능을 작은 단위 서비스들로 쪼개서 각각 독립적으로 배포/운영한다
- RestController 함수형 <-> 객체지향,마이크로로 쓸려고
